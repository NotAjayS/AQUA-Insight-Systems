// Placeholder JS for future logic
document.addEventListener("DOMContentLoaded", () => {
  // Example: set sample values
  document.getElementById("weekly-value").innerText = "Weekly";
  document.getElementById("monthly-value").innerText = "Monthyl";
  document.getElementById("yearly-value").innerText = "6,000 L";

  // You can add click events here later
});
